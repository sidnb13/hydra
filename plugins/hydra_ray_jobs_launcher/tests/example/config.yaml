defaults:
  - override hydra/launcher: ray_jobs

task: 1

hydra:
  launcher:
    entrypoint_num_cpus: 1  # Number of CPUs to reserve per job
    entrypoint_num_gpus: 0  # Number of GPUs to reserve per job
    entrypoint_memory: null # Memory to reserve per job (e.g. "1GB")